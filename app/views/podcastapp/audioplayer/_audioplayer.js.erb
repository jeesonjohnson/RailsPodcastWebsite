// Replaces the podcastapp navigation bar with id #music-player-controls with an
// a music player object
$('#music-player-controls').html("<%= j(render 'podcastapp/audioplayer/audioplayer.html') %>")
