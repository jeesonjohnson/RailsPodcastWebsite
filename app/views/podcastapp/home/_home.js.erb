// Jquery commnad alongisde AJAX request, to render appropriate user partial,
// to provide the user with a seemless experiance, by rendering the associated partial.
$('#main-document-class').html("<%= j(render 'podcastapp/home/home.html') %>")
